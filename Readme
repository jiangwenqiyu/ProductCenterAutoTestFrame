测试用例字段说明
    name: 用例名称
    path: 路径
    header: 请求头
    method: 请求方式   post 、 get
    data: 入参
    dataType: 入参类型   json、form
    param: url上的参数
    assert: 断言   [ {'jmespath': '通过jmespath取返回值的数据', 'exp': '期望值', 'type':'判断类型  eq 相等  in  期望值被包含在返回值里（exp in res）  not in 期望值不被包含在返回值里（exp not in res）' } ]
    save: 对返回值进行保存  可为空，如果有数据，则在请求完成后会自动根据jmespath，提取res的数据，保存在 productEnv.saveValue里    {'用例名称': '保存的数据'}


